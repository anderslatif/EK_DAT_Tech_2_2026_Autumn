<div class="title-card">
    <h1>Welcome to Tech 2</h1>
</div>

---

# About the Course

The semester plan.

---

# PR Please

Make a PR to the semester repository with any suggestions for improvement, additions, corrections etc. Even minor typos are welcomed. Nothing is too small.

**Please DO IT.**

---

# Create a Semester Repository

**Success Criteria**: You have a repository with a `README.md` file in it.

We will clone it in in a later slide.

---

# Terminal Refresher

```bash
cd <directory>    # change directory
cd ..             # go up one directory
ls                # list files in the current directory
mkdir <name>      # create a new directory
rm <file>         # remove a file
touch <file_name> # create a file 
rm -r <dir>       # remove a directory and its contents
cp <source> <destination>  # copy a file or directory
mv <source> <destination>  # move a file or directory
```

## Challenges

1. Create a new directory called `tech` in your home directory.

2. Create a new file called `techinfo` inside the `tech` directory.

3. Create a new directory called `tech2` inside the `tech` directory.

4. Go to your home directory in one step.

5. Delete the `tech` directory and all its contents.

---

# Git Refresher

Cloning

```bash
git clone <repository_url>    # clone a repository
```

1. Adding

    ```bash
    git add -A                # stage all changes for commit
    git add <file/directory>  # stage a file / directory for commit
    ```

2. Committing

    ```bash
    git commit -m "<message>" # commit staged changes with a message
    ```

3. Pushing

    ```bash
    git push                  # push changes to the remote repository
    ```

---

# Git Challenge

1. Clone the semester repository to your local machine.

2. Add text to the `README.md` file.

3. ???

4. ???

5. ???

6. Success! Look in the GitHub UI to confirm your changes.

 